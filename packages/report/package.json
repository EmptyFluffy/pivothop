{
  "name": "@pivothop/report",
  "private": true,
  "version": "0.0.1",
  "description": "Route report PDF generation (Phase 3). Puppeteer over an HTML template on the site's design system.",
  "type": "module",
  "engines": {
    "node": ">=20"
  }
}

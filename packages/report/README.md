# @pivothop/report

Phase 3 package. Renders the route report PDF with Puppeteer from an HTML template that uses the same two typefaces, palette, and voice as the site (`docs/01-style-direction.md`). Contents per `docs/03-mvp-scope.md`: cover with route summary, role decoded, 90-day plan, evidence checklist, bridge role if applicable, graph snapshot, salary map.

Not built yet — Phase 1 (the scrape) comes first. This package exists so the workspace shape is stable.
